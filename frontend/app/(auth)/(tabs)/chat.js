import { View, Text } from "react-native";
import React from "react";

const Chat = () => {
  return (
    <View className="flex items-center justify-center h-full">
      <Text className="dark:text-white font-bold text-xl">Chat</Text>
    </View>
  );
};

export default Chat;
