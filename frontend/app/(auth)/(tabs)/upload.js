import { View, Text } from "react-native";
import React from "react";

const Upload = () => {
  return (
    <View className="flex items-center justify-center h-full">
      <Text className="dark:text-white font-bold text-xl">Upload</Text>
    </View>
  );
};

export default Upload;
